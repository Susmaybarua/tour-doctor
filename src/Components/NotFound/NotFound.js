import React from 'react';

const NotFound = () => {
    return (
        <div>
            <p>Not found the page,Error 404, Access denied.</p>
            <h5>What happened?</h5>
            <p>This website is using a security service to protect itself from online attacks.</p>
        </div>
    );
};

export default NotFound;